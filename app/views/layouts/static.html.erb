<%= extends :application do %>

  <%= replace :main do %>

    <div class="<%= container_classes %>">

      <%= render :partial=>'/flash_msg', layout: 'shared/flash_messages' unless flash.empty? %>

      <div class="row">
        <%= yield %>
      </div>

    </div>

  <% end %>

<% end %>