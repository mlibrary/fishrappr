<div class="container">
    <div class="col-md-12">
        <pre>AHOY</pre>
        <pre><%= hathitrust_image_src(@page) %></pre>

        <p>
            <%= hathitrust_image(@page, size: '500,') %>
        </p>

        <p>
            <%= hathitrust_thumbnail(@page) %>
        </p>

    </div>
</div>
